Just like in JavaScript, you can debug by printing values.

function App() {
  const name = "Pathik";
  console.log("Debug → name is:", name); // Debugging

  return <h1>Hello {name}</h1>;
}

Open Browser DevTools → Console tab
Output
Debug → name is: Pathik
